name: generic_shop_app_data
description: "A new Flutter package project."
version: 0.0.1
homepage: https://github.com/ljmatan
publish_to: none

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  generic_shop_app_architecture:
    path: ../arch
  generic_shop_app_api:
    path: ../api
  generic_shop_app_services:
    path: ../services

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
