/// Application visual interface elements and their respective lifecycle handling is defined with this library.
///
/// The `common` folder is defined with all of the shareable or global-level components, as well as with the theme configuration,
/// while the `routes` folder is specified with each individual [Route](https://api.flutter.dev/flutter/widgets/Route-class.html) object.

library view;
